{# I see no reason to change this structure. May adjust it and add diff css later. #}

<nav class="navbar navbar-expand-lg bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Pokemon Battle!</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">Register</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/pokedex">Pokedex</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/battle">Battle</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/help">Help</a>
                    </li>
                </ul>
            </div>   

            {# show user (login, register, ) on navbar code goes here: #}

            <ul class="navbar-nav ms-auto align-items-center">
                {# if not logged in #}
                {% if current_user.is_anonymous %}
                <li class="nav-item">
                    <a class="nav-link" href="{{url_for('auth.login')}}">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{url_for('auth.register')}}">Register</a>
                </li>
                {# if logged in #}
                {% else %}
               `    {# see icon #}
                    <img src="{{current_user.get_icon_url()}}" style="height:50px">
                <li>
                    {# see edit #}
                    <div class="row"> 
                        <div class="col-12">
                            <a href="{{url_for('auth.edit_profile')}}">
                                <i class="bi bi-gear-fill"></i>
                            </a>
                        </div>
                        {# see logout #}
                        <div class="col-12">
                            <a href="{{url_for('auth.logout')}}">
                                <i class="bi bi-box-arrow-right"></i>
                            </a>
                        </div>
                    </div>
                </li>
                {% endif %}
            </ul>  

        </div>
    </nav> 
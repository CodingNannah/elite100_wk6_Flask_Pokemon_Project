{# displaying pokemon from pokemon database to user #}

{% extends './base.html.j2' %}

{% block css %}
    <link rel="stylesheet" href="{{url_for('static', filename = 'css/pokemon.css')}}">
{% endblock %}

{% block title %}
    Pokemon!
{% endblock %}

{% block content %}
<h1>Pokemon!</h1>

<h3>Need some inspiration?</h3>

<p> 
Check out this list of potential Pokemon to add to your Pokedex:
https://www.pokemon.com/us/pokedex/
</p>

{# Where do I place this?
"List" of Pokemon the User may search for is here https://www.pokemon.com/us/pokedex/ #}


{# !!!!!!!!!!!!!CHANGE THIS ergast.html INFO TO POKEMON!!!!!!!!!!!!!! #}
<form action = "{{url_for('main.get_pokemon')}}" method="POST">
   <div class="mb-3">
    <label for="name" class="form-label">Name of Pokemon</label>
    <input type="text" class="form-control" name="name" id="name" >
  </div>
  <button type="submit" class="btn btn-primary">Submit</button>
  <br>
  <small style="color:red">{{ error }}</small>
</form>
{% if pokemon %}
    <table class="table table-striped table-hover table-dark mt-5">
        <thead>
            <tr>
                <th scope="col">Position</th>
                <th scope="col">First Name</th>
                <th scope="col">Last Name</th>
                <th scope="col">Win</th>
                <th scope="col">DOB</th>
                <th scope="col">Nationality</th>
                <th scope="col">Constructor</th>
            </tr>
        </thead>
        <tbody>
        {% for poke in pokemon %}
            <tr>
                    <th scope="row">{{ racer["position"] }}</td>
                    <td>{{ racer.first_name }}</td>
                    <td>{{ racer.last_name }}</td>
                    <td>{{ racer.wins }}</td>
                    <td>{{ racer.DOB }}</td>
                    <td>{{ racer.nationality }}</td>
                    <td>{{ racer.constructor }}</td>
                </tr>
            {% endfor %}
        </tbody>
    <table>
{% endif %}
{% endblock %}